# PROTOCOL V2 APPLIED

This project/version is maintained under the user-supplied **AI PROJECT KNOWLEDGE & ENGINEERING LINEAGE PROTOCOL — V2**.

Rules applied to v0.2.7-R2:
- Preserve `User Request → Previous State → Implementation → Build/CI → Runtime → Bug/Regression → Fix/Revert → Known-Good → Current Source`.
- Never equate BUILD PASS / CI PASS with RUNTIME PASS.
- Preserve meaningful failed attempts: CI #55 EOL transport failure and CI #56 accidental clean-helper deletion are retained in history/evidence.
- Use explicit evidence labels and `UNKNOWN` / `RUNTIME UNTESTED` when live evidence is missing.
- Keep per-version history, feature history, BUG/DEC/EVID registries, PROJECT_KNOWLEDGE and CHANGELOG current with source changes.
- Protect strange-but-proven behavior, especially clean-v0.2.7 F4 pause, unless comparative runtime evidence or an explicit user request supports changing it.
- Preserve expensive technical facts instead of forcing future AI to rediscover them.
- The complete original user-supplied Protocol V2 document is included verbatim in the **v0.2.7-R2 source release ZIP** for handoff to future AI.
